# Shopify App Configuration
# This file configures your Shopify app for deployment and integration
# Learn more: https://shopify.dev/docs/apps/tools/cli/configuration

# The client_id (API Key) is public and safe to commit to version control
# The client_secret (API Secret) is stored securely in Replit Secrets as SHOPIFY_API_SECRET
# Access credentials in your code using process.env.SHOPIFY_API_KEY and process.env.SHOPIFY_API_SECRET

name = "Spiral"
client_id = "edf4e15492cfecf8934f5084e4f8e686"
application_url = "https://spiral-app-1.replit.app"
embedded = true

[auth]
redirect_urls = ["https://spiral-app-1.replit.app/shopify/callback"]

[access_scopes]
scopes = "read_products,read_orders,write_discounts,read_fulfillments"
